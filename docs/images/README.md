# Screenshots

Drop the demo screenshot here as `demo-placeholder.png` (the root README links to it).

Suggested shot: `/demo` mid-call, with the transcript showing a Hinglish exchange on the left and the
requirements panel on the right with Intent, Configuration, Location and Budget chips filled —
ideally captured just after a mid-call revision so the Budget chip shows the updated figure.
